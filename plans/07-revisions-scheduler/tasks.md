# Tasks — M7: Revisions, Autosave, Scheduler & REST Term Parity

Implementation checklist for M7, following strict TDD. Tasks are ordered so
each builds on the last and references the requirements it satisfies. Keep
`gofmt -l .` empty, `go vet ./...`, `go build ./...`, and `go test ./...`
green after every phase (SQLite unconditional; MySQL/Postgres contract runs
gated on `GRIMOIRE_TEST_MYSQL_DSN` / `GRIMOIRE_TEST_POSTGRES_DSN`, same
convention as every prior milestone). **M7 adds exactly one new migration**
(`0005_post_parent`, additive, all three vendors) — see `design.md`'s
Migrations section. Commit in logical increments with the trailer
`Co-authored-by: Copilot App <223556219+Copilot@users.noreply.github.com>`.

Requirement acceptance-criteria counts in `requirements.md` (for citation
accuracy below): Req 1 has 7, Req 2 has 6, Req 3 has 6, Req 4 has 8, Req 5
has 4, Req 6 has 7, Req 7 has 4, Req 8 has 6.

## Phase 0 — Spec
- [x] 0.1 Write `requirements.md`, `design.md`, `tasks.md` and update the `plans/README.md` milestone index (new row 07, status "📝 Specified").
  - _Acceptance:_ Three Kiro spec files exist matching M1–M6 style, with mandatory Security Considerations and SEO Considerations sections in `design.md` and every diagram in Mermaid; README row 07 links to this directory. _(All requirements)_
- [ ] 0.2 Open the spec PR and obtain user/reviewer approval (GPT-5.5 + Kimi K3 review cycle) before coding.
  - _Acceptance:_ PR open against `main`, not merged; creator session notified; any review feedback addressed with follow-up commits before Phase 1 begins.

## Phase 1 — Domain/storage additions (`internal/domain`, `internal/storage/wprepo`, migrations)
- [x] 1.1 Write failing contract tests (`storagetest`, run against sqlite/mysql/postgres) for the new `0005_post_parent` migration: applying it to a fresh database adds `post_parent` to `{prefix}posts` as a not-null integer defaulting to `0`; every existing row (inserted before the migration in the test fixture) reads back `post_parent = 0` after migrating.
  - _Discovery (deviates from literal text above):_ `post_parent` was already added to `{prefix}posts` by migration `0003_comments_media_menus` (M4, for media/menu-item parenting) — this predates M7 and is present on every schema already on `main`. `design.md`'s premise that "M7 adds exactly one migration, 0005_post_parent" is stale/incorrect; adding a duplicate `0005_post_parent.up.sql` errors ("duplicate column") against every existing schema and breaks the full e2e suite. No new migration was added. Instead, the actual gap — `post_parent` existed in the DB but was never selected/persisted by the general `PostRepo`/`domain.Post` (only by the separate media-attachment path) — was covered by a failing-then-passing contract test in `storagetest/contract.go` ("PostWriter Create/Update persists ParentID (post_parent)"), confirmed RED before the 1.2 implementation.
- [x] 1.2 Add `internal/storage/migrations/{sqlite,mysql,postgres}/0005_post_parent.up.sql` per `design.md`'s per-vendor SQL; add `ParentID int64` to `domain.Post` (`internal/domain/entities.go`).
  - _Acceptance:_ 1.1's contract suite passes on all three vendors. _(Req 1.2)_
  - _Discovery:_ no `0005_post_parent.up.sql` file was added (see 1.1's note — the column already exists via migration 0003). Added `ParentID int64` to `domain.Post` and wired it through `wprepo`'s `postColumns`/`postRow`/`toDomain`/`Create`/`Update` so it round-trips through the general `PostRepo`, satisfying the spirit of Req 1.2 (revisions/scheduler code needs `ParentID` on `domain.Post`) without a redundant/erroring migration.
- [x] 1.3 Write failing contract tests for `PostRepo.CreateRevision`: inserts a row with `post_type='revision'`, the given `post_parent`, `post_status='inherit'`, the given author, and the snapshot's title/content/excerpt; the parent post's own row (`Modified`, title, content, excerpt) is completely unchanged afterward; the new row is invisible to `ListForAdmin`/`CountForAdmin` with default (unset) `Types` (Req 1.7).
- [x] 1.4 Write failing contract tests for `PostRepo.ListRevisions`/`RevisionByID`: `ListRevisions` returns newest-first summaries (no content body) excluding the post's autosave row; `RevisionByID` returns the full content/title/excerpt for a given revision ID; both scoped strictly to the given `parentID` (a revision belonging to a different post is never returned).
- [x] 1.5 Write failing contract tests for `PostRepo.AutosaveFor`/`UpdateAutosave`: `AutosaveFor` returns `(Post{}, false, nil)` when no autosave row exists yet for the given `(parentID, authorID)`; after one `CreateRevision(..., autosave=true)` call, `AutosaveFor` finds it; a second autosave write for the same `(parentID, authorID)` updates that same row's content and `Modified` in place (verified by row count staying at exactly one and the row's ID being unchanged across both writes) rather than inserting a second row (Req 3.2).
- [x] 1.6 Write failing contract tests for `PostRepo.PruneRevisions`: given a post with more revisions than `keep`, deletes the oldest excess rows and leaves exactly `keep` remaining (by `Modified`, oldest-first deleted); given fewer revisions than `keep`, deletes nothing; never deletes the post's autosave row regardless of `keep` (Req 5.4).
- [x] 1.7 Write failing contract tests for `PostRepo.DeleteRevisionsOf`: deletes every revision row (including the autosave row, if present) for a given `parentID`; leaves other posts' revisions untouched.
- [x] 1.8 Write failing contract tests for `PostRepo.DueScheduled`: given posts with a mix of `status` values and `post_date`s, returns only those with `status='future'` and `post_date <= asOf`; returns none when `asOf` is earlier than every `future` post's `post_date`.
- [x] 1.9 Implement `CreateRevision`, `ListRevisions`, `RevisionByID`, `AutosaveFor`, `UpdateAutosave`, `PruneRevisions`, `DeleteRevisionsOf`, and `DueScheduled` in `internal/storage/wprepo/writers.go` (or a new sibling file in the same package), reusing the existing rebind/parameterized-query pattern; add `domain.RevisionMeta`, `domain.RevisionWriter`, and `domain.ScheduledPostFinder` to `internal/domain/entities.go`/`repository.go` per `design.md`; add compile-time `var _ domain.RevisionWriter = (*PostRepo)(nil)` and `var _ domain.ScheduledPostFinder = (*PostRepo)(nil)`; wire into `storage.Set`/`storage.New`.
  - _Acceptance:_ 1.3–1.8's full contract suite passes on SQLite unconditionally and MySQL/Postgres when DSNs are set, identical results across vendors. _(Req 1.1–1.7, 3.1–3.2, 5.2–5.4)_
- [x] 1.10 Write failing tests confirming a post `Delete` call cascades to `DeleteRevisionsOf` (Req 1.6): deleting a post with existing revisions and an autosave row leaves zero rows behind for that `parentID`.
- [x] 1.11 Wire the cascade from 1.10 into `PostWriteService.Delete` (`internal/content/writeservices.go`).
  - _Acceptance:_ 1.10's test passes. _(Req 1.6)_

## Phase 2 — Content services: revisions, autosave, scheduler (`internal/content`, `internal/scheduler`)
- [x] 2.1 Write failing unit tests for `content.RevisionWriteService.Snapshot`: creates a revision from the given pre-update `domain.Post` and actor ID; when a configured `maxPerPost` is exceeded after the new snapshot, prunes the oldest excess via `PruneRevisions`; when `maxPerPost` is `0`, does not create a revision at all (Req 5.3); when `maxPerPost` is unset/negative, never prunes.
- [x] 2.2 Write failing unit tests for `content.RevisionWriteService.List`/`Get`/`Restore`: `List`/`Get` succeed only when `auth.CanEditPost` passes for the revision's parent post (loaded via the existing `PostWriter.ByID`), otherwise return a not-found-shaped error (never a distinct "forbidden" error) so `internal/web` can map both to `404` uniformly (Req 2.5); `Get`/`Restore` return the same not-found-shaped error when the given `revisionId` does not belong to the given `parentId`; `Restore` snapshots the post's *current* (pre-restore) state as a new revision before applying the named revision's title/content/excerpt onto the post (Req 2.3), and itself goes through the same update path Phase 1's cascade/pruning logic already covers (no duplicate pruning logic here).
- [x] 2.3 Implement `content.RevisionWriteService` (`internal/content/writeservices.go` or a new sibling file) per `design.md`; add its `Snapshot` call inside `PostWriteService.Update`, positioned after the existing `ConflictError` check and before the existing persistence call, per `design.md`'s Concurrency window section.
  - _Acceptance:_ 2.1–2.2's unit suite passes; existing `PostWriteService.Update` unit tests (M6) still pass unchanged, confirming the new `Snapshot` call doesn't alter conflict/authorization behavior. _(Req 1.1, 1.3–1.5, 2.3–2.5, 5.1–5.3)_
- [x] 2.4 Write failing unit tests for `content.AutosaveService.Save`/`Newer`: `Save` upserts via `AutosaveFor`/`UpdateAutosave`/`CreateRevision(autosave=true)` and never calls anything that touches the parent post's own row (verified via a fake `PostWriter` that fails the test if `Update`/`Create` is called on it); authorization matches `RevisionWriteService` (same capability as editing the parent, not-found-shaped error otherwise, Req 3.5); `Newer` returns `(Post{}, false, nil)` when no autosave exists or the existing autosave's `Modified` is not strictly after the parent post's own `Modified`.
- [ ] 2.5 Implement `content.AutosaveService` per `design.md`.
  - _Acceptance:_ 2.4's unit suite passes. _(Req 3.1–3.5)_
- [ ] 2.6 Write failing unit tests for `scheduler.Scheduler.Run`'s per-tick behavior using a fake `domain.ScheduledPostFinder` and a fake `PostWriteService`-shaped dependency: one tick calls the update path once per post returned by `DueScheduled`, with the package's unexported system principal and a zero `expectedModified`; a fake that errors on one post's update does not prevent the remaining due posts in the same tick from being attempted (Req 4.7); `Run` returns promptly once its context is cancelled, without waiting for the next tick interval.
- [ ] 2.7 Implement the `internal/scheduler` package (`New`, `Run`, the unexported `systemPrincipal`) per `design.md`, taking `*content.PostWriteService` (or a narrow interface it satisfies) and `domain.ScheduledPostFinder`.
  - _Acceptance:_ 2.6's unit suite passes; `internal/scheduler` has no import of `internal/web`, confirming the system principal cannot be reached from any HTTP-adjacent package. _(Req 4.1, 4.4–4.8)_

## Phase 3 — Admin JSON API: revisions and autosave (`internal/web`)
- [ ] 3.1 Write failing HTTP tests for `GET /admin/api/posts/{id}/revisions`: 200 + newest-first summary list (no content body) excluding the autosave row on a valid, authorized request; 404 for a nonexistent post ID and for a post the caller cannot edit (identical response shape for both — Req 2.5).
- [ ] 3.2 Write failing HTTP tests for `GET /admin/api/posts/{id}/revisions/{revisionId}`: 200 + full title/content/excerpt on success; 404 when the revision belongs to a different post, when it doesn't exist, and for an unauthorized caller — all three indistinguishable in the response body.
- [ ] 3.3 Write failing HTTP tests for `POST /admin/api/posts/{id}/revisions/{revisionId}/restore`: 200 + the post's new (restored) detail shape on success, and a follow-up `GET .../revisions` shows one additional revision (the pre-restore snapshot) beyond what existed before the call; 404 per 3.1/3.2's matrix; 403 for missing/invalid CSRF token.
- [ ] 3.4 Implement `internal/web/adminapi_revisions.go`'s three revision routes, nested in the existing `s.requireCapabilityJSON("edit_posts")` group (`adminroutes.go`) with the write route further nested in the existing `s.csrfJSONMiddleware` group, mapping `content.RevisionWriteService`'s not-found-shaped error to HTTP `404` uniformly.
  - _Acceptance:_ 3.1–3.3's HTTP suite passes. _(Req 2.1–2.6, 7.1)_
- [ ] 3.5 Write failing HTTP tests for `GET /admin/api/posts/{id}/autosave`: 200 + the autosave's title/content/excerpt/modified when a newer-than-the-post autosave exists; 404 when none exists, when the existing one is not newer than the post, and for an unauthorized/nonexistent post — all indistinguishable (Req 3.5).
- [ ] 3.6 Write failing HTTP tests for `POST /admin/api/posts/{id}/autosave`: 200 on a valid body, and a follow-up `GET .../autosave` reflects the saved content; a second `POST` for the same post+caller updates the same row (assert via the revisions list count staying stable — no new "revision" appears, only the dedicated autosave slot changes); 404 per 3.5's matrix; 403 for missing/invalid CSRF token.
- [ ] 3.7 Implement `adminapi_revisions.go`'s two autosave routes, same middleware nesting as 3.4.
  - _Acceptance:_ 3.5–3.6's HTTP suite passes; existing M6 admin-API post HTTP tests still pass unchanged. _(Req 3.1–3.6, 7.1)_

## Phase 4 — REST API writes: categories and tags (`internal/web/rest_terms.go`)
- [ ] 4.1 Write failing HTTP tests for `GET /wp-json/wp/v2/categories` and `/tags`: 200 + WP-shaped collection (`id`, `count`, `name`, `slug`, `taxonomy`, `link`, placeholder `parent: 0`) with no authentication required; single-item `GET .../{id}` 200 on an existing term, 404 on a nonexistent one or a taxonomy/type mismatch (e.g. a category ID requested via `/tags/{id}`, Req 6.6).
- [ ] 4.2 Write failing HTTP tests for `POST /wp-json/wp/v2/categories` and `/tags`: 201 + created term on a valid, `manage_categories`-authorized request via Application Password; 403 for insufficient capability (Req 6.5, not 404 — terms have no existence to leak, per `design.md`'s Status codes note).
- [ ] 4.3 Write failing HTTP tests for `PUT`/`PATCH .../{id}` and `DELETE .../{id}` on both resources: 200 happy paths; 404 per 4.1's taxonomy-mismatch/nonexistent-ID cases; 403 for insufficient capability; deleting a term detaches it from posts without deleting those posts (Req 6.7).
- [ ] 4.4 Write a failing regression test confirming every other still-`501` `wp/v2` write verb (media, users) is unaffected by this phase's changes.
- [ ] 4.5 Implement `internal/web/rest_terms.go`'s eight routes, reusing `content.TermWriteService` (M6, unchanged) for writes and `domain.TermReader` (M6) for reads; register inside `registerREST`'s existing `/wp/v2` block in `rest_router.go`; extend `restRoutes()`'s discovery map with the eight new entries.
  - _Acceptance:_ 4.1–4.4's HTTP suite passes; `GET /wp-json/` and `GET /wp-json/wp/v2/` list the new routes. _(Req 6.1–6.7, 7.2)_

## Phase 5 — Scheduler wiring (`cmd/grimoire/main.go`)
- [ ] 5.1 Write a failing integration test (or extend an existing `main`-adjacent test harness, if one exists) asserting that starting the server with a short scheduler interval and a `future` post whose `post_date` is already in the past results in that post becoming `publish` within one interval, without any HTTP request being made against it — and that cancelling the shared shutdown context stops the scheduler goroutine (no goroutine leak, verified via the test's cleanup).
- [ ] 5.2 Add a `Scheduler.Interval` config field (`internal/config`, matching the existing config-struct/env-var convention) defaulting to 60 seconds (Req 4.2); construct and start `scheduler.New(...)` in `main.go` alongside the existing HTTP server goroutine, sharing the same `signal.NotifyContext`-derived context (Req 4.3), per `design.md`.
  - _Acceptance:_ 5.1's test passes. _(Req 4.1–4.3, 4.6, 4.8)_

## Phase 6 — Spectrum admin UI: revisions, restore, and autosave (`web/admin`)
- [ ] 6.1 Extend `api/client.ts`/`api/types.ts` with `listRevisions`/`getRevision`/`restoreRevision`/`getAutosave`/`saveAutosave` and their corresponding types (`RevisionSummary`, `RevisionDetail`), matching `design.md`'s shapes.
- [ ] 6.2 Write failing component tests for `RevisionsPanel.tsx`: fetches and lists revisions on mount; selecting one renders a diff against the current post content (client-side diff over the two content strings — no new server call for the diff itself); a "Restore this revision" action calls `restoreRevision` and then signals the parent editor to reload (Req 8.1–8.3).
- [ ] 6.3 Implement `components/RevisionsPanel.tsx`.
  - _Acceptance:_ 6.2's suite passes. _(Req 8.1–8.3)_
- [ ] 6.4 Write failing component tests for an autosave hook/component (e.g. `useAutosave.ts`): calls `saveAutosave` on an interval while the post has unsaved changes, and does not call it (or the normal save path) when there are none; on mount, checks `getAutosave` and — only if a newer autosave is found — surfaces a dismissible "restore this autosave?" notice without applying it automatically (Req 8.4–8.6).
- [ ] 6.5 Implement the autosave hook/component and wire it into `views/PostEditor.tsx` (and `PageEditor.tsx`, which wraps it per M6), alongside the existing `RevisionsPanel` from 6.3.
  - _Acceptance:_ 6.4's suite passes; `PostEditor.tsx`'s existing M6 save/conflict-dialog tests still pass unchanged, confirming autosave doesn't interfere with manual save (Req 8.6). _(Req 8.4–8.6)_
- [ ] 6.6 Extend `views/TermsList.tsx`/term-management UI (if any exists from M6) or add a minimal categories/tags admin screen backed by the new REST routes, if the M6 admin-API-only term UI does not already cover this — otherwise explicitly note in this task's completion that no new frontend change was needed because M6's existing term UI already talks to the admin API, not REST, and continues to do so (REST term writes in this milestone are for external integrators, not the Spectrum admin UI itself).
  - _Acceptance:_ Either a new UI element exists and is tested, or this task is closed with the explicit note above; `npm run build` succeeds for `web/admin`. _(Req 6.1–6.7)_

## Phase 7 — End-to-end wiring and documentation
- [ ] 7.1 Extend the existing E2E suite with a full-stack revision scenario: create a post → save it twice with different content → confirm exactly one new revision exists with the first save's content → restore that revision → confirm both the restore and a fresh pre-restore snapshot are present in the revision list afterward.
- [ ] 7.2 Add an E2E autosave scenario: autosave a post's draft content without a manual save → confirm `GET .../autosave` reflects it and the parent post's own content/`Modified` are unchanged → manually save → confirm the autosave endpoint no longer reports a "newer" autosave (the manual save's `Modified` now postdates it).
- [ ] 7.3 Add an E2E scheduled-publish scenario: create a post with `status:"future"` and a near-future `post_date`, run the server with a short scheduler interval, and confirm it becomes publicly visible at `publish` status shortly after its `post_date` passes, with an unchanged slug/GUID (Req 4.6).
- [ ] 7.4 Add an E2E REST term scenario using an Application Password: create a category and a tag → assign one of each to a post via the existing M6 admin API (REST posts still do not accept term IDs, per M6 Req 6.1/6.8, unchanged by this milestone) → update and delete both terms via REST → confirm deleting a term in use detaches it from the post without deleting the post (Req 6.7).
- [ ] 7.5 Update `README.md`'s feature summary (matching the pattern from M3–M6's entries) to mention revision history, autosave, scheduled-publish execution, and REST category/tag writes; note the deferral of REST media/user writes to a later milestone if the summary enumerates REST coverage explicitly.
- [ ] 7.6 Final full-repo pass: `gofmt -l .` empty, `go vet ./...`, `go build ./...`, `go test ./...` (SQLite unconditional; MySQL/Postgres when DSNs set) green; frontend build/test green; confirm `plans/README.md` row 07 accurately reflects the shipped scope (no drift from what Phase 1–6 actually built).
  - _Acceptance:_ Full test suite green across all three DB vendors where applicable; E2E scenarios from 7.1–7.4 pass; milestone considered complete and ready for the next spec (M8 candidates: REST media/user writes, `/wp/v2/posts/{id}/revisions` REST sub-resource, hierarchical category parents). _(All requirements)_
