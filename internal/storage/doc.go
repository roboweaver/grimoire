// Package storage contains database adapters that implement the domain
// repository interfaces, one sub-package per supported vendor (mysql, postgres,
// sqlite), plus per-dialect migrations.
package storage
